name: Build Images
on: [push]
jobs:

  build-job:
    name: Build
    runs-on: ubuntu-latest
    steps:

    - name: Checkout code
      uses: actions/checkout@v1

    - name: Build image
      run: make build
      env:
        REPO: master
        TAG: master